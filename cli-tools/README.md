# CLI tools

Malleable eBook Reader does not currently require any project-managed command-line
tools. This directory is retained so future tools can follow the same download,
verification, installation, and activation pattern as `prog-langs/`.
